apiVersion: v1
kind: Service
metadata:
  name: {{ include "remove-backend.fullname" . }}
  labels:
    {{- include "remove-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "remove-backend.selectorLabels" . | nindent 4 }}
